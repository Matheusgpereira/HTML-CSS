function adicionarFilme() {
    var filmeFavorito = document.getElementById('filme').value //.getElementById = função de pegar um documento pelo id, .value = pegar o valor do id
    alert(filmeFavorito);
}